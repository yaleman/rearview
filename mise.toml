[env]
_.file = { path = ".envrc", redact = true }

[tasks.models]
description = "Download and verify bundled model files"
run = "./scripts/fetch-models.sh"
sources = ["scripts/fetch-models.sh"]
outputs = [
  "models/SmolVLM-500M-Instruct-Q8_0.gguf",
  "models/mmproj-SmolVLM-500M-Instruct-Q8_0.gguf",
]

[tasks.sim]
description = "Build and launch on the iPhone 17 Pro simulator"
depends = ["models"]
run = "./scripts/sim.sh"

[tasks."device:build"]
description = "Build a signed Release app for the configured device"
depends = ["models"]
run = "./scripts/device.sh build"

[tasks."device:install"]
description = "Install the current Release app on the configured device"
depends = ["device:build"]
run = "./scripts/device.sh install"

[tasks."device:launch"]
description = "Launch the installed app on the configured device"
depends = ["device:install"]
run = "./scripts/device.sh launch"

[tasks.device]
description = "Build, install, and launch on the configured device"
depends = ["models"]
run = "./scripts/device.sh deploy"

[tasks."test:testflight-packaging"]
description = "Validate the internal TestFlight export configuration"
run = "./scripts/test-testflight-export-options.sh"

[tasks."testflight:internal"]
description = "Archive and upload a uniquely numbered TestFlight Internal Only build"
depends = ["models", "test:testflight-packaging"]
run = "./scripts/upload-testflight-internal.sh"

[tasks.esp]
dir = "esphome"
run = ["uvx esphome run rearview-indicator.yaml"]


[tasks.both]
depends = ["device"]
run = "mise run esp"
